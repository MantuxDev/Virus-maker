Requirements:
- C++ compiler (eg g++ which is part of MinGW)
- `code.cpp` file

Steps to compile `code.cpp` file from terminal:
1) Right-click on an empty space in the folder
2) Click on `Open in terminal`
3) Copy this to terminal: g++ -o code.exe code.cpp -mwindows